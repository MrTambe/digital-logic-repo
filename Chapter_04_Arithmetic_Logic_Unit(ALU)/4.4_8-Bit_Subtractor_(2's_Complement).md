# ➖ 4.4 8-Bit Subtractor (2's Complement)

In digital hardware design, building a completely separate circuit just to perform subtraction is highly inefficient. It wastes silicon real estate and increases manufacturing costs. 

Instead, engineers use a brilliant mathematical shortcut: **Computers don't actually subtract; they only add.** By converting a positive number into its negative equivalent using **2's Complement**, a standard 8-Bit Adder can perform subtraction without changing its core internal wiring. 

Mathematically, the operation $A - B$ is simply rewritten as $A + (-B)$.

---

## 🧮 4.4.1 The Math: 2's Complement Logic

To make the adder perform subtraction, we must first convert the operand $B$ into negative $B$ ($-B$). In the binary world, we achieve this using the 2's Complement method, which is a two-step process.

<table width="100%">
  <tr>
    <th width="35%" align="center">🎬 Conceptual Operation</th>
    <th width="65%" align="center">📊 2's Complement Breakdown</th>
  </tr>
  <tr>
    <td align="center" valign="top" style="padding: 10px;">
      <img src="media/2s_complement_concept.png" alt="2s Complement Conversion" style="width: 100%; border-radius: 8px;" />
      <br><br>
      <i style="font-size: 0.9em;">To subtract B from A, we invert B and add a 1 to the final equation.</i>
    </td>
    <td align="left" valign="top" style="padding: 10px; font-size: 0.95em;">
      <b>The 2-Step Conversion Rule:</b>
      <ol>
        <li><b>1's Complement (Invert):</b> Flip every `0` to a `1`, and every `1` to a `0`. This is the mathematical equivalent of NOT $B$ ($\overline{B}$).</li>
        <li><b>Add 1:</b> Add a single binary `1` to the inverted result.</li>
      </ol>
      <b>The Subtraction Equation:</b><br>
      If $A - B = A + (-B)$, and $-B = \overline{B} + 1$, then we can substitute to get the final hardware equation:<br><br>
      <div style="text-align: center; font-size: 1.1em; background-color: rgba(0, 0, 0, 0.05); padding: 8px; border-radius: 5px;">
        $A - B = A + \overline{B} + 1$
      </div>
      <br>
      <i>To build a subtractor, our hardware must be able to flip the B-bits and inject a +1 into the math.</i>
    </td>
  </tr>
</table>

---

## 🔄 4.4.2 The XOR Trick (Controlled Inverter)

We need a way to selectively invert the $B$ inputs *only* when we want to subtract. If we use standard NOT gates, the circuit will be permanently stuck in subtraction mode. 

The solution is to use an **Exclusive-OR (XOR) gate** as a switchable, controlled inverter.

<table width="100%">
  <tr>
    <th width="35%" align="center">🎬 The Controlled Inverter</th>
    <th width="65%" align="center">🛠️ XOR Truth Table & Logic</th>
  </tr>
  <tr>
    <td align="center" valign="top" style="padding: 10px;">
      <img src="media/xor_controlled_inverter.gif" alt="XOR Gate as a Controlled Inverter" style="width: 100%; border-radius: 8px;" />
      <br><br>
      <i style="font-size: 0.9em;">A single SUB control line dictates whether the XOR gate passes the data through normally, or flips it.</i>
    </td>
    <td align="center" valign="top" style="padding: 10px;">
      <table style="width: 100%; text-align: center; border-collapse: collapse;">
        <thead>
          <tr>
            <th style="border-bottom: 2px solid #ccc; padding: 6px;">Control (SUB)</th>
            <th style="border-bottom: 2px solid #ccc; padding: 6px;">Input ($B$)</th>
            <th style="border-bottom: 2px solid #ccc; padding: 6px;">XOR Output</th>
            <th style="border-bottom: 2px solid #ccc; padding: 6px;">Result</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td style="padding: 6px;"><code>0</code> (Add)</td>
            <td style="padding: 6px;"><code>0</code></td>
            <td style="padding: 6px; background-color: rgba(0, 255, 0, 0.05);"><b><code>0</code></b></td>
            <td rowspan="2" style="padding: 6px;">Passed Unchanged</td>
          </tr>
          <tr>
            <td style="padding: 6px;"><code>0</code> (Add)</td>
            <td style="padding: 6px;"><code>1</code></td>
            <td style="padding: 6px; background-color: rgba(0, 255, 0, 0.05);"><b><code>1</code></b></td>
          </tr>
          <tr>
            <td style="padding: 6px; border-top: 2px solid #ccc;"><code>1</code> (Subtract)</td>
            <td style="padding: 6px; border-top: 2px solid #ccc;"><code>0</code></td>
            <td style="padding: 6px; border-top: 2px solid #ccc; background-color: rgba(255, 0, 0, 0.05);"><b><code>1</code></b></td>
            <td rowspan="2" style="padding: 6px; border-top: 2px solid #ccc;"><b>Inverted!</b> ($\overline{B}$)</td>
          </tr>
          <tr>
            <td style="padding: 6px;"><code>1</code> (Subtract)</td>
            <td style="padding: 6px;"><code>1</code></td>
            <td style="padding: 6px; background-color: rgba(255, 0, 0, 0.05);"><b><code>0</code></b></td>
          </tr>
        </tbody>
      </table>
    </td>
  </tr>
</table>

---

## 🛠️ 4.4.3 Putting It Together: The 8-Bit Adder/Subtractor

By combining a standard 8-Bit Adder, eight XOR gates on the $B$ inputs, and a single overarching **SUB/ADD** control wire, we create a unified arithmetic circuit that can do both addition and subtraction dynamically.

<table width="100%">
  <tr>
    <th width="35%" align="center">🎬 The Unified Architecture</th>
    <th width="65%" align="center">🛠️ Wiring Diagram & Operation</th>
  </tr>
  <tr>
    <td align="center" valign="top" style="padding: 10px;">
      <img src="media/8bit_adder_subtractor_circuit.png" alt="8-Bit Adder Subtractor Circuit" style="width: 100%; border-radius: 8px;" />
      <br><br>
      <i style="font-size: 0.9em;">Notice how the SUB wire connects to every XOR gate AND the $C_{IN}$ pin of the very first Full Adder.</i>
    </td>
    <td align="left" valign="top" style="padding: 10px; font-size: 0.95em;">
      <b>Architecture Setup:</b>
      <ul>
        <li><b>The A Inputs:</b> $A_0$ through $A_7$ wire directly into the adder block normally.</li>
        <li><b>The B Inputs:</b> $B_0$ through $B_7$ each pass through an XOR gate before entering the adder.</li>
        <li><b>The Control Line (SUB):</b> A single wire is tied to one leg of <i>every</i> XOR gate, and is also connected to the initial <b>Carry-In ($C_{IN}$)</b> pin of the Adder block.</li>
      </ul>
      <br>
      <b>How it works during Addition (SUB = 0):</b><br>
      The XOR gates pass $B$ unchanged. The $C_{IN}$ receives a `0`. <br>
      Equation: $A + B + 0 \rightarrow$ <b>Standard Addition</b>.
      <br><br>
      <b>How it works during Subtraction (SUB = 1):</b><br>
      The XOR gates invert $B$ to $\overline{B}$ (1's complement). The $C_{IN}$ receives a `1` (which provides the crucial "+1" for 2's complement).<br>
      Equation: $A + \overline{B} + 1 \rightarrow$ <b>Exact Subtraction!</b>
    </td>
  </tr>
</table>
